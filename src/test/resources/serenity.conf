
base.url {
  api = "#{base.url.environments.stg}"
  environments {
    dev = "https://automatedconsulcore-dev.arreglatech.com"
    stg = "https://automatedconsulcore-dev.arreglatech.com"
    prd = "https://automatedconsulcore-dev.arreglatech.com"
  }
}

serenity {
  # FOR_EACH_ACTION
  # BEFORE_AND_AFTER_EACH_STEP
  # AFTER_EACH_STEP
  # FOR_FAILURES
  take.screenshots = AFTER_EACH_STEP

  # level logging trace
  logging = VERBOSE

  # encode build report
  report.encoding = "utf8"
  encoding = "UTF-8"

  # project name in the report
  project.name = "Test Automated Consult"

  //miliseconds
  #step.delay = 500
}
